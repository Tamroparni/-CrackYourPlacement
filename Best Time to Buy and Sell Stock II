class Solution {
    public int maxProfit(int[] prices) {
        int max = 0;
        int p = prices[0];
        for(int i = 1;i<prices.length; i++){
            if(p < prices[i]) max += prices[i] - p;
            p = prices[i];
        }
        return max;
    }
}
